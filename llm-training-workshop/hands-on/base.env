# =============================================================================
# LLM Fine-Tuning MLOps Pipeline - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp base.env .env
#
# The .env file is gitignored and will not be committed.
# =============================================================================

# -----------------------------------------------------------------------------
# HuggingFace Hub (Required for publishing)
# -----------------------------------------------------------------------------
# Your HuggingFace API token (get one at https://huggingface.co/settings/tokens)
# Required scopes: write access
HF_TOKEN="hf_xxxxxxxxxxxxxxxxxxxx"

# Target repository for model publishing (format: username/repo-name)
HF_REPO_ID="your-username/your-model-name"

# -----------------------------------------------------------------------------
# GPU Configuration (Optional)
# -----------------------------------------------------------------------------
# Select which GPU(s) to use (comma-separated for multi-GPU)
# Default: "0"
# CUDA_VISIBLE_DEVICES="0"

# -----------------------------------------------------------------------------
# HuggingFace Cache (Optional)
# -----------------------------------------------------------------------------
# Directory for caching downloaded models and datasets
# Default: ~/.cache/huggingface
# HF_HOME="/path/to/cache"

# Legacy cache variable (use HF_HOME instead)
# TRANSFORMERS_CACHE="/path/to/cache"

# -----------------------------------------------------------------------------
# Remote Training via SSH (Optional)
# -----------------------------------------------------------------------------
# These can also be configured in the Makefile directly
#
# SSH username for remote GPU server
# REMOTE_USER="username"

# Remote GPU server IP or hostname
# REMOTE_HOST="192.168.1.100"

# Project directory on remote server
# REMOTE_DIR="~/Dev/my-project"

# -----------------------------------------------------------------------------
# LakeFS Data Versioning (Optional)
# -----------------------------------------------------------------------------
# Only needed if using LakeFS for data versioning
#
# LakeFS server endpoint
# LAKEFS_ENDPOINT="http://localhost:8000"

# LakeFS access credentials
# LAKEFS_ACCESS_KEY="your-access-key"
# LAKEFS_SECRET_KEY="your-secret-key"

# -----------------------------------------------------------------------------
# MLflow Tracking (Optional)
# -----------------------------------------------------------------------------
# MLflow tracking server URI
# MLFLOW_TRACKING_URI="http://localhost:5050"

# Default experiment name
# MLFLOW_EXPERIMENT_NAME="llm-fine-tuning"
